apiVersion: v1
kind: Service
metadata:
  name: games-recommendation-test-service
spec:
  selector:
    app: games-recommendation-test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5009
  type: LoadBalancer
